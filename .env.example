NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SITE_URL=

# Optional: enables AI flyer reading on /submit-event. Without this set,
# the image upload field works normally but skips the auto-fill step.
ANTHROPIC_API_KEY=
# Optional override; defaults to claude-3-5-sonnet-latest if unset.
ANTHROPIC_MODEL=
